import EventsListed from "../ui/events";
import MainHeader from "../ui/header";
export default function Events() {
    return (
        <main>
            <MainHeader />
            <EventsListed />
        </main>
    );
}
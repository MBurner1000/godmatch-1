import PickImages from "../ui/pick-images";

export default function Images() {
    return (
        <main className="flex justify-center items-center min-h-screen">
            <PickImages />
        </main>
    );
}
